apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: grpc-app
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
