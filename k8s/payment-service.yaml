apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: grpc-app
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
